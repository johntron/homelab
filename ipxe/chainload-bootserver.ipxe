#!ipxe

echo Pausing so you can read...
sleep 5 ||

echo before DHCP
imgstat ||
show ip ||
show mac ||
show dns ||
show gateway ||
show next-server ||
show filename ||
show asset ||

sleep 5 ||

echo DHCP
dhcp ||
show ip ||
show mac ||
show dns ||
show gateway ||
show next-server ||
show filename ||
show asset ||

echo Config
config ||

echo Chainload
chain http://${next-server:string}:8000/?mac=${net0/mac:uristring} ||


echo Shell
shell ||